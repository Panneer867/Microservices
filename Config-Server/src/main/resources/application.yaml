server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: 'git@github.com:Panneer867/spring-boot-microservice-config.git'
          default-label: master
          cloneOnStart: true
        
