server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          branch: master
          uri: git@github.com:Panneer867/spring-boot-microservice-config.git
          cloneOnStart: true
          bootstrap: true
